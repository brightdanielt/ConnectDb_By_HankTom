<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.danielt.connectdb_by_hanktom.Simple_browse_diary"
    tools:showIn="@layout/activity_simple_browse_diary"
    android:weightSum="1"
    android:background="@drawable/blue_background"
    android:foreground="@drawable/blue_foreground">

    <DatePicker
        android:layout_width="wrap_content"
        android:layout_height="100dp"
        android:id="@+id/datePicker"
        android:layout_centerHorizontal="true"
        android:calendarViewShown="false"
        android:datePickerMode="spinner"
        android:layout_marginTop="@dimen/margin_to_bg" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/listView"
        android:layout_below="@+id/datePicker"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="@dimen/big_margin_to_bg"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:layout_marginRight="@dimen/margin_to_bg"
        android:dividerHeight="-12.0sp"
        android:layout_marginLeft="@dimen/margin_to_bg" />

</RelativeLayout>
