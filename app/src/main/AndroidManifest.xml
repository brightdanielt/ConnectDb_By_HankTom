<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.danielt.connectdb_by_hanktom">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--網路連線-->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--檢查網路狀態-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/about_us"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:windowSoftInputMode="adjustPan">
        <activity
            android:name=".Create_diary"
            android:label="@string/title_activity_create_diary"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".Calendar"
            android:label="@string/title_activity_calendar"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".Check_friend_invitation" />
        <activity
            android:name=".Create_friend_invitation"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".Simple_browse_diary"
            android:label="@string/title_activity_simple_browse_diary"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".Load_friend_list"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <!-- android:windowSoftInputMode="stateHidden" -->
        <activity
            android:name=".Load_friend_info"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".Lobby"
            android:label="@string/title_activity_loby"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- android:theme="@style/AppTheme"> -->
            <!-- android:theme="@style/AppTheme.NoActionBar"> -->

        </activity>

        <activity android:name=".Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Register"
        android:windowSoftInputMode="adjustPan">

        </activity>
    </application>

</manifest>